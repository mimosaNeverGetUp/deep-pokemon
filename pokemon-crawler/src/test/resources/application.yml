spring:
  cache:
    type: NONE
  data:
    mongodb:
      database: test
eureka:
  client:
    enabled: false

logging:
  level:
    com:
      mimosa:
        deeppokemon:
          tagger:
            PokemonAttackDefenseTagProvider: debug